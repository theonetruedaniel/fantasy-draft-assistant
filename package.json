{
  "name": "fantasy-draft-companion",
  "version": "0.1.5",
  "private": true,
  "type": "module",
  "description": "Read-only Yahoo and ESPN fantasy football draft companion",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "build": "node scripts/build.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.28.2",
    "jsdom": "^30.0.1"
  }
}
